# ──────────────────────────────────────────────────────────────────────────
#  Local secrets — NEVER commit this file (it's in .gitignore).
#  Copy .env.example → .env and fill the values below.
# ──────────────────────────────────────────────────────────────────────────

# GitHub Personal Access Token with `repo` scope.
# Used by electron-builder to publish releases to
# https://github.com/ANICKON-PRO/BG3-ULTIMA/releases
# Create one at: https://github.com/settings/tokens
GH_TOKEN=ghp_replace_me
